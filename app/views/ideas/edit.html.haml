#intro.section
  .header
    - if user_signed_in?
      %h3
        %em Share Ideas Now
    - else
      %h3
        %em Get Started Now
        %small or
        = link_to "Sign Up", new_user_registration_path
  .body#submit-idea
    = render :partial => 'ideas/edit'
