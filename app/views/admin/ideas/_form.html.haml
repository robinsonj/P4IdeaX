= form_for @admin_idea do |f|
  - if @admin_idea.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_idea.errors.count, "error")} prohibited this admin_idea from being saved:"
      %ul
        - @admin_idea.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
